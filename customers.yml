cubes:
  - name: customers
    sql_table: customers

    dimensions:
      - name: customer_id
        sql: customer_id
        type: string
        primary_key: true

      - name: customer_name
        sql: customer_name
        type: string

    measures:
      - name: count
        type: count
